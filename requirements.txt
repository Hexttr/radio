# Pirate Radio AI - Dependencies

# Core
asyncio-atexit>=1.0.0
aiohttp>=3.9.0

# News scraping
feedparser>=6.0.0
beautifulsoup4>=4.12.0

# AI (Groq или Ollama через OpenAI-совместимый API)
groq>=0.4.0
openai>=1.0.0

# Text-to-Speech
edge-tts>=6.1.0

# Audio processing (FFmpeg called via subprocess)
# FFmpeg must be installed separately

# Web server for streaming
aiohttp>=3.9.0

# Utilities
python-dotenv>=1.0.0
