# Диагностика радио

## Логи в реальном времени

При запуске `python -m src.radio` в консоли отображаются:

- `➕ ADD: file.mp3 (queue=N)` — добавление в плейлист
- `▶️ PLAY: file.mp3 (bytes)` — начало воспроизведения
- `🎤 DJ:` — добавление реплики диджея
- `🎵 MUSIC:` — добавление трека

---

## 1. Почему нет фраз диджея?

**По логам: фразы добавляются и воспроизводятся.**  
Порядок: jingle → intro → **DJ** → track → **DJ** → track...

Если вы их не слышите, возможные причины:
- Файлы очень короткие (2–5 сек) — можно не заметить
- Низкая громкость — проверьте в системе и в браузере
- Проблемы с буфером или сетью — попробуйте обновить страницу

**Проверка:** В логах должны быть строки `▶️ PLAY: xxxxx.mp3` для hash-файлов (реплики из кеша TTS).

---

## 2. Почему нет новостей?

**Ошибка Groq API: `403 Forbidden`**

Возможные причины:
1. **Неверный или истёкший API-ключ** — проверьте `.env`, зайдите на [console.groq.com](https://console.groq.com)
2. **Геоограничения** — Groq может блокировать регионы (в т.ч. РФ)
3. **Исчерпан лимит** — бесплатный план ограничен

**Решение 1:** Обновите `GROQ_API_KEY` в `.env` или используйте VPN.

**Решение 2 — Ollama (локально, без облака):**
1. Установите [Ollama](https://ollama.com)
2. Скачайте модель: `ollama pull llama3.2`
3. В `.env` задайте: `AI_BACKEND=ollama`
4. Запустите радио — новости и погода будут генерироваться локально.

---

## 3. Почему нет погоды?

**Было:** `wttr.in` часто блокирует запросы или отвечает с ошибками.

**Сделано:** Добавлен запасной источник — **Open-Meteo** (бесплатный, без ключа).

Сейчас используется Open-Meteo для Москвы и Белграда. Если погода по‑прежнему не приходит, проверьте логи на строку `🌤️ Weather fetch error`.
